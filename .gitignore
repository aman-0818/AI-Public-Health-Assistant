# Secrets — never commit API keys
.env

# Python cache/virtualenv
__pycache__/
*.pyc
venv/

# Local model/output artifacts — generated by training scripts
model_artifact.json
ml_output/

# Raw dataset files — large/sensitive, not for version control
*.xlsx
dataset/*.csv
