# ─────────────────────────────────────────────
# Disease Risk Sentinel — Python Dependencies
# ─────────────────────────────────────────────
# Install all with: pip install -r requirements.txt
# Recommended: Python 3.10+

# ── Server (api_server.py) ────────────────────
# Uses Python standard library only — no extra installs needed for the server.

# ── ML Training (train_ml_model.py) ──────────
xgboost>=2.0.0          # XGBoost classifier for disease risk prediction
scikit-learn>=1.3.0     # Train/test split, metrics, LabelEncoder
imbalanced-learn>=0.11  # SMOTE for minority class oversampling
pandas>=2.0.0           # Data loading and preprocessing
openpyxl>=3.1.0         # Required by pandas to read .xlsx files
matplotlib>=3.7.0       # Plotting training loss curves and feature importance
seaborn>=0.12.0         # Confusion matrix heatmap
numpy>=1.24.0           # Numerical operations
